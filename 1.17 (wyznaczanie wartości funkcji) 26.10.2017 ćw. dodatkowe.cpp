#include <iostream>
#include <math.h>

using namespace std;

int main()

{
{
double
x;
    cout<<"Podaj x"<<endl;
    cin>>x;


    if (x<7)
        cout<<((x*x*x)+1)<<endl;
        else
            if (x==7)
            cout<<(cos(x-1))<<endl;
        else
            if (x==9)
            cout<<(sqrt(3*x))<<endl;
        else
            cout<<(-8*x)<<endl;
}
return 0;
}
